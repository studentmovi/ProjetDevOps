name: Build TripSchool App

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    # ====================================================
    #  CHECKOUT
    # ====================================================
    - name: Checkout repository
      uses: actions/checkout@v4

    # ====================================================
    #  PYTHON
    # ====================================================
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Upgrade pip
      run: |
        python -m pip install --upgrade pip

    - name: Install dependencies
      run: |
        pip install -r app/requirements.txt
        pip install pyinstaller

    # ====================================================
    #  VERSION
    # ====================================================
    - name: Read app version
      id: app_version
      shell: pwsh
      run: |
        $v = Get-Content app/version.txt
        "version=$v" >> $env:GITHUB_OUTPUT

    # ====================================================
    #  BUILD MAIN (ONEDIR)
    # ====================================================
    - name: Build main (onedir)
      shell: pwsh
      run: |
        cd app
        pyinstaller --onedir --noconsole --clean --noupx `
          --collect-submodules utils `
          --collect-submodules views `
          --collect-submodules controller `
          --collect-submodules popups `
          --add-data "assets;assets" `
          --add-data "version.txt;." `
          --add-data "changelog.json;." `
          main.py

    # ====================================================
    #  BUILD LAUNCHER (ONEDIR)
    # ====================================================
    - name: Build launcher (onedir)
      shell: pwsh
      run: |
        cd app
        pyinstaller --onedir --noconsole --clean --noupx `
          --collect-submodules utils `
          --collect-submodules views `
          --add-data "assets;assets" `
          --add-data "version.txt;." `
          --add-data "changelog.json;." `
          launcher.py

    # ====================================================
    #  RELEASE FOLDER
    # ====================================================
    - name: Prepare release folder
      shell: pwsh
      run: |
        New-Item -ItemType Directory -Force release | Out-Null

        # onedir => dist\main\main.exe et dist\launcher\launcher.exe
        Copy-Item -Recurse -Force app\dist\main release\main
        Copy-Item -Recurse -Force app\dist\launcher release\launcher

        Copy-Item app\version.txt release\
        Copy-Item app\changelog.json release\

    # ====================================================
    #  ZIP
    # ====================================================
    - name: Create ZIP archive
      shell: pwsh
      run: |
        Compress-Archive `
          -Path release `
          -DestinationPath "pytrip-${{ steps.app_version.outputs.version }}-windows.zip"

    # ====================================================
    #  GITHUB RELEASE
    # ====================================================
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        files: pytrip-${{ steps.app_version.outputs.version }}-windows.zip

    # ====================================================
    #  DISCORD — SUCCESS
    # ====================================================
    - name: Notify Discord (success)
      if: success()
      shell: pwsh
      run: |
        $repoUrl = "https://github.com/${{ github.repository }}"
        $releaseUrl = "$repoUrl/releases/tag/${{ github.ref_name }}"
        $commitUrl = "$repoUrl/commit/${{ github.sha }}"
        $timestamp = [System.DateTime]::UtcNow.ToString("yyyy-MM-ddTHH:mm:ss.fffZ")
        $commitMsg = "${{ github.event.head_commit.message }}" -replace '"', '\"'

        $payload = @{
          username = "TripSchool Builder"
          embeds = @(
            @{
              title = "🚀 Nouvelle version ${{ steps.app_version.outputs.version }} publiée"
              url = $releaseUrl
              description = "Build Windows terminé avec succès et release créée automatiquement."
              color = 5763719
              fields = @(
                @{ name="Version"; value="${{ github.ref_name }}"; inline=$true },
                @{ name="Auteur"; value="${{ github.actor }}"; inline=$true },
                @{ name="Commit"; value="[$commitMsg]($commitUrl)"; inline=$false },
                @{ name="Téléchargement"; value="[Télécharger la version Windows]($releaseUrl)"; inline=$false }
              )
              footer = @{ text="TripSchool CI • Windows" }
              timestamp = $timestamp
            }
          )
        } | ConvertTo-Json -Depth 10

        Invoke-RestMethod `
          -Uri "${{ secrets.DISCORD_WEBHOOK_URL }}" `
          -Method Post `
          -ContentType "application/json" `
          -Body $payload

    # ====================================================
    #  DISCORD — FAILURE (DÉTAILLÉ)
    # ====================================================
    - name: Notify Discord (failure)
      if: failure()
      shell: pwsh
      run: |
        $repoUrl = "https://github.com/${{ github.repository }}"
        $runUrl = "$repoUrl/actions/runs/${{ github.run_id }}"
        $commitUrl = "$repoUrl/commit/${{ github.sha }}"
        $timestamp = [System.DateTime]::UtcNow.ToString("yyyy-MM-ddTHH:mm:ss.fffZ")

        $payload = @{
          username = "TripSchool Builder"
          avatar_url = "https://cdn-icons-png.flaticon.com/512/919/919833.png"
          embeds = @(
            @{
              title = "❌ Build Windows échoué"
              description = "Le pipeline GitHub Actions a échoué. Voir les détails ci-dessous."
              color = 15548997
              fields = @(
                @{ name="Repository"; value="${{ github.repository }}"; inline=$true },
                @{ name="Branche / Tag"; value="${{ github.ref_name }}"; inline=$true },
                @{ name="Auteur"; value="${{ github.actor }}"; inline=$true },
                @{ name="Commit"; value="[Voir le commit]($commitUrl)"; inline=$false },
                @{ name="Logs du build"; value="[Ouvrir le run GitHub Actions]($runUrl)"; inline=$false }
              )
              footer = @{ text="TripSchool CI • Failure" }
              timestamp = $timestamp
            }
          )
        } | ConvertTo-Json -Depth 10

        Invoke-RestMethod `
          -Uri "${{ secrets.DISCORD_WEBHOOK_URL }}" `
          -Method Post `
          -ContentType "application/json" `
          -Body $payload
